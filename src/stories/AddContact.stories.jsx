import React from 'react';

import AddContact from '../components/AddContact';

export default {
    title: 'AddContact',
    component: AddContact,
  };

  const Template = (args) => <AddContact />;
  
  export const Primary = Template.bind({});